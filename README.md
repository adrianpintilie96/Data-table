## In-memory data table written in C++.
### Provides the ability to dinamically add rows, columns, take set unions, differences, intersections and combines tables that have different property columns.

